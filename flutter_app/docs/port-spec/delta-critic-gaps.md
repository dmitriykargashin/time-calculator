# RemoveADS delta - completeness critic gaps/resolutions

- UNACCOUNTED FILE: gradle/wrapper/gradle-wrapper.properties — Gradle wrapper changed from gradle-5.4.1-all.zip to gradle-6.7-bin.zip (and 'https\://' unescaped to 'https://'). Required by the AGP 3.5.2 -> 4.2.0-alpha16 upgrade that the monetization spec does capture. No port impact, but no spec mentions it.

- UNACCOUNTED FILES (5): .idea/assetWizardSettings.xml, .idea/codeStyles/Project.xml, .idea/compiler.xml (bytecodeTargetLevel target="11"), .idea/jarRepositories.xml (new), .idea/misc.xml — Android Studio IDE metadata only (icon-wizard history, code style, compiler level, maven repo cache). Zero runtime/port impact; safe to ignore, but no spec accounts for them. With these 6 + the 61 spec-covered files, all 68 changed files are now accounted for.

- CONTRADICTION (resolved): ui-theme-redesign §5 says String.toHTMLWithGrayColor(context) has 'no size change'; engine-delta §6 says it now applies a 0.7f relative-size span. End-state Extension.kt is `spannable { size(0.7f, color(ContextCompat.getColor(context, R.color.colorResultNums), this)) }` — the ENGINE SPEC IS CORRECT: numbers/operators in toLightSpannableString (result line, format/per previews) render at 0.7x relative size on the branch (master gray was full size). Flutter port must apply the 0.7 size factor to gray spans.

- MISSING DETAIL (all specs): the selected-format label wiring. Branch CalculatorActivity.kt:661 sets `tvFormats.text = it.textPresentationOfTokens` as PLAIN TEXT inside the viewModel.getSelectedFormat() observer (the `.toHTMLBlackColor()` call is commented out); master styled the old buttonFormats with `.toHTMLWithLightGreenColor()`. So the label shows e.g. 'Hour Minute' or 'All Units' colored solely by its XML textColor @color/colorControls at @dimen/separator_time_text_size (18sp base / 14sp land), no span, no size factor. The observer still closes the formats overlay collapsing toward the screen center (commonConstraintLayout w/2, h/2) when it is visible — unchanged from master.

- MINOR NUMBERING MISMATCH (not a real conflict, verified against end-state ResultFormatsRepository.kt): engine-delta says 'entry #4 Year Month Day Minute is REPLACED' (1-based) while persistence-state says 'entry 3 becomes Year Month Day Hour, new entry at index 4' (0-based). Ground truth: 0-based index 3 = 'Year Month Day Hour' (preview '1 Year 2 Month 3 Day 4 Hour'), 0-based index 4 = NEW 'Year Month Day Hour Minute' (preview '1 Year 2 Month 3 Day 4 Hour 5 Minute'), default-selected 'Hour Minute' at 0-based index 18, 24 entries total. Both specs agree in substance; Flutter dev should use the 0-based reading.

- MINOR MISCOUNT: ui-theme-redesign §7 says 'five 200dp-wide default-styled Buttons' on the support screen but then correctly lists six ids; the end-state view_support_app.xml has SIX 200dp Buttons (btnSupport1, btnSupport3, btnSupport5, btnSupport9, btnSupportRate, btnShareTheApp). The monetization spec §3.2 describes the count correctly (4 buy + rate + share).

- VERIFIED (no gap, recorded for the parent): plural rule identical in all specs and source (append ASCII 's' to the 8 time-keyword types when value.compareTo(BigDecimal.ONE) != 0, scale-insensitive); app/src/test and app/src/androidTest have ZERO diff vs master, and master's WhenCalculateExpression.kt uses Token(TokenType.NUMBER, "5") and Token(TokenType.HOUR) — both constructor shapes no longer exist on the branch, confirming the test-module compile break; SKU list verbatim at CalculatorActivity.kt:65-72 = ["support_1","support_3","support_5","support_9","remove_ads"] with support_15/support_29 only in comments; the ONLY app-code SharedPreferences usage is PrefRepository with PREFERENCE_NAME="MY_APP_PREF" and key PREF_THEME_COLOR="PREF_THEME_COLOR" storing "0"/"1"/"2" (awesome-app-rating keeps its own internal prefs as both specs note); theme mapping 0->FOLLOW_SYSTEM/1->NO/2->YES verified in onRadioButtonClicked and the getPrefThemeColor observer; light/dark color tables and all three dimens buckets match the UI spec verbatim; build.gradle deltas (minSdk 21 all flavors, free 20/"2.0.5", pro 6/"1.0.4" unchanged, billing 3.0.3+ktx, awesome-app-rating 2.3.0, firebase-bom 26.8.0 + analytics-ktx, removed firebase-ads 18.3.0 / android-rate 1.0.1 / facebook-android-sdk 5.13.0, appcompat 1.2.0, constraintlayout 2.0.4, material 1.4.0-alpha01, lifecycle-extensions 2.2.0 / viewmodel-ktx 2.3.0, AGP 4.2.0-alpha16, Kotlin 1.4.10, google-services 4.3.4) all match the monetization spec; layout-land/activity_main.xml == layout-sw600dp/activity_main.xml and layout-land/view_per.xml == layout-sw600dp/view_per.xml confirmed byte-identical via git blob hashes; AndroidManifest delta is exactly +ACCESS_NETWORK_STATE, -AdMob APPLICATION_ID meta-data, -facebook.sdk.ApplicationId meta-data; ic_star_green_24dp pre-exists on master unchanged; onPause is empty and onResume calls checkPurchases(); all 8 commit messages are reflected in the specs including the a6470ba->994973d trailing-zero lineage warning.


## Verdict
PASS with minor gaps. The four delta specs are collectively exhaustive and mutually consistent on everything that matters for the Flutter port: every behavioral, UI, persistence, and monetization change in the 68-file diff is captured, the plural rule / zero-total fix / 24-format list / SKU list / pref key / theme mapping / dependency table were all verified verbatim against branch end-state source, and all 8 commit messages are reflected. Findings: 6 changed files unaccounted (gradle-wrapper.properties + five .idea metadata files — none port-relevant), one genuine contradiction (toHTMLWithGrayColor DOES gain a 0.7f size span; engine spec correct, UI spec wrong), one missing wiring detail (tvFormats is set as plain unstyled text from the getSelectedFormat observer, dropping master's light-green span), plus two cosmetic spec slips (1-based vs 0-based format-entry numbering, 'five' vs six support-screen buttons). Apply the gaps list as an addendum and the combined spec is safe to implement from.
